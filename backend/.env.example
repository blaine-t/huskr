# ── Database ─────────────────────────────────────────────────────────────────
DATABASE_URL=sqlite:app.db

# ── Azure / Microsoft Entra ID ────────────────────────────────────────────────
# Application (client) ID from your Azure App Registration
AZURE_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Client secret created under Certificates & Secrets
AZURE_CLIENT_SECRET=your-client-secret-here

# Directory (tenant) ID – use "common" to allow any tenant / personal accounts
AZURE_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# Must match the Redirect URI registered in your Azure App Registration exactly.
# During development this is typically the backend URL.
REDIRECT_URL=http://localhost:3000/auth/callback

# ── Frontend ──────────────────────────────────────────────────────────────────
# Base URL of the Leptos SPA. The backend:
#   • Sets this as the CORS allow-origin (with credentials).
#   • Redirects the browser here after a successful login or logout.
#   • Accepts ?next=/some/path on /auth/login to redirect post-auth to a
#     specific frontend route.
FRONTEND_URL=http://localhost:3001

# ── Logging ───────────────────────────────────────────────────────────────────
# RUST_LOG=info
